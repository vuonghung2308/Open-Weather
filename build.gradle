buildscript {
    apply from: "versions.gradle"
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath deps.gradle_tools
        classpath deps.kotlin_plugin
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}